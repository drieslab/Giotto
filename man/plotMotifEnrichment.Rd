% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_motif_visuals.R
\name{plotMotifEnrichment}
\alias{plotMotifEnrichment}
\title{plotMotifEnrichment}
\usage{
plotMotifEnrichment(
  gobject,
  motif_result,
  p_thresh = 0.05,
  min_observed = 5,
  top = 15L,
  facet_topology = TRUE,
  show_plot = NULL,
  return_plot = NULL,
  save_plot = NULL,
  save_param = list(),
  default_save_name = "plotMotifEnrichment"
)
}
\arguments{
\item{gobject}{giotto object}

\item{motif_result}{output of [cellProximityMotifs()]}

\item{p_thresh}{adjusted p-value threshold for calling a motif significant}

\item{min_observed}{drop motif classes seen fewer than this many times.
Rare classes carry unstable effect sizes and dominate the axes.}

\item{top}{number of most significant motifs to label}

\item{facet_topology}{draw one panel per topology}

\item{show_plot}{logical. show plot}

\item{return_plot}{logical. return ggplot object}

\item{save_plot}{logical. save the plot}

\item{save_param}{list of saving parameters, see
\code{\link{showSaveParameters}}}

\item{default_save_name}{default save name for saving, don't change,
change save_name in save_param}
}
\value{
ggplot
}
\description{
Volcano-style triage plot of motif enrichment: effect size against
significance, sized by how often the motif actually occurs, split by
topology.

This is the plot for deciding which motifs are worth pursuing out of the
thousands a size-4 run produces. Enriched and depleted motifs appear in one
frame, and point size keeps a huge z-score built on three occurrences from
reading like a finding.
}
\examples{
\dontrun{
g <- GiottoData::loadGiottoMini("visium")
m <- cellProximityMotifs(g, cluster_column = "leiden_clus", size = 3L)
plotMotifEnrichment(g, m)
}
}
