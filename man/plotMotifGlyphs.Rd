% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_motif_visuals.R
\name{plotMotifGlyphs}
\alias{plotMotifGlyphs}
\title{plotMotifGlyphs}
\usage{
plotMotifGlyphs(
  gobject,
  motif_result,
  top = 12L,
  order_by = c("z", "p_adj", "observed"),
  direction = c("enriched", "depleted", "both"),
  min_observed = 5,
  ncol = 4L,
  cell_color_code = NULL,
  show_plot = NULL,
  return_plot = NULL,
  save_plot = NULL,
  save_param = list(),
  default_save_name = "plotMotifGlyphs"
)
}
\arguments{
\item{gobject}{giotto object}

\item{motif_result}{output of [cellProximityMotifs()]}

\item{top}{number of motifs to draw}

\item{order_by}{`"z"`, `"p_adj"` or `"observed"`}

\item{direction}{which motifs to draw: `"enriched"`, `"depleted"` or
`"both"`}

\item{min_observed}{drop motif classes seen fewer than this many times}

\item{ncol}{columns in the grid}

\item{cell_color_code}{optional named vector of cell type colours. Defaults
to the object's discrete palette, so glyphs match the spatial plots.}

\item{show_plot}{logical. show plot}

\item{return_plot}{logical. return ggplot object}

\item{save_plot}{logical. save the plot}

\item{save_param}{list of saving parameters, see
\code{\link{showSaveParameters}}}

\item{default_save_name}{default save name for saving, don't change,
change save_name in save_param}
}
\value{
ggplot
}
\description{
Draws each motif as the little graph it actually is -- nodes coloured by
cell type, edges as drawn -- in a grid ranked by effect size.

A `motif_id` like `size4_paw_A-B-B-C` is not readable at a glance; the same
motif as four coloured dots joined the way the tissue joins them is.
}
\examples{
\dontrun{
g <- GiottoData::loadGiottoMini("visium")
m <- cellProximityMotifs(g, cluster_column = "leiden_clus", size = 3L)
plotMotifGlyphs(g, m, top = 12)
}
}
