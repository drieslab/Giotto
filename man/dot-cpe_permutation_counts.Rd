% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_interaction.R
\name{.cpe_permutation_counts}
\alias{.cpe_permutation_counts}
\title{.cpe_permutation_counts}
\usage{
.cpe_permutation_counts(
  from,
  to,
  codes,
  K,
  number_of_simulations,
  set_seed = TRUE,
  seed_number = 1234
)
}
\arguments{
\item{from, to}{integer vectors of 1-based node indices, one entry per
(deduplicated, undirected) edge.}

\item{codes}{integer vector of cell-type codes (1..K), one entry per node.}

\item{K}{integer, number of cell-type levels.}

\item{number_of_simulations}{number of label permutations.}

\item{set_seed, seed_number}{seed control.}
}
\value{
list with `obs` (integer vector of length K*K) and `sim`
(K*K x number_of_simulations integer matrix) of pair counts, indexed by
`(lo - 1) * K + hi` for the sorted type pair `(lo, hi)`.
}
\description{
Vectorized cell-type-pair counting for the observed network
and for a set of node-label permutations.
}
\details{
The null model permutes cell-type labels over the *nodes* of the
network, so each cell keeps a single label across all of its edges. Counting
is done with [tabulate()] over an integer pair key, so no intermediate
edge x simulation table is ever materialized.
}
\keyword{internal}
